import React from 'react';
import { Text } from 'react-native';
import styled from 'styled-components/native';

const Container = styled.View``;

export default () => {
    return (
        <Container>
            <Text>Página TEMPORÁRIA</Text>
        </Container>
    );
}